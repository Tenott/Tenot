# Секретный ключ для подписи JWT токенов (обязательно сменить на production!)
JWT_SECRET=your_super_secret_key_here_change_in_production

# PostgreSQL credentials (для локальной разработки можно оставить как есть)
# Для production рекомендуется сменить на более безопасные значения
POSTGRES_USER=tenot
POSTGRES_PASSWORD=tenot
POSTGRES_DB=tenot
